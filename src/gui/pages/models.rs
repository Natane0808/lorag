//! G6: model management page (download / status for LLM, embedding, rerank).
//!
//! The real implementation lives at [`crate::gui::models`]. This module is
//! retained so the `pub mod models;` in [`super::mod`] stays well-formed and
//! the per-page directory structure from G4 keeps looking uniform — same
//! pattern as G5's service page.
