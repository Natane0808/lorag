//! G11: about page is implemented in [`crate::gui::about`] (top-level module,
//! not under `pages/`). This module is retained as a doc-only stub so the
//! `pub mod about;` in [`super::mod`] does not break during G4→G11 handoff.
//!
//! The real page lives at `src/gui/about.rs`.
